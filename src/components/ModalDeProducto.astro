<!-- ---
const { producto, onClose } = Astro.props;
---

<div class="fixed inset-0 bg-black bg-opacity-50 flex justify-center items-center z-50">
  <div class="bg-white p-8 rounded-lg w-96 relative">
    <button 
      class="absolute top-2 right-2 text-gray-600 text-2xl"
      on:click={onClose}>
      ✖️
    </button>
    <img src={producto.imagenPrincipal} alt={producto.titulo} class="w-full rounded-lg mb-4" />
    <h2 class="text-2xl font-bold mb-4">{producto.titulo}</h2>
    <p class="text-gray-600 mb-6">{producto.descripcion}</p>
    <a 
      href="https://wa.me/1234567890" 
      target="_blank"
      class="bg-green-500 text-white px-6 py-2 rounded-lg flex items-center justify-center">
      <img src="/path/to/whatsapp-icon.png" alt="WhatsApp" class="w-6 h-6 mr-2" />
      Contactar por WhatsApp
    </a>
  </div>
</div> -->
