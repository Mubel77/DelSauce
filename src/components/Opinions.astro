<!-- ---
import Stars from "./Stars.astro"
const {opinions} = Astro.props

---


<div class={`bg-[#D4E4FE] py-10  mt-20`}>
    <h2 class="text-center mb-14 text-2xl font-semibold sm:text-4xl md:text-4xl">{opinions.titulo}</h2>
       
    <div class="flex flex-wrap gap-x-4 gap-y-14 items-center w-full px-5" >
        {opinions.listaDeDatos.map((item:any, i: number) => (
            <div class="p-4 max-w-md mx-auto bg-blue-50 rounded-lg shadow-md" data-aos="fade-up">
                <div class="flex items-center mb-4">
                    {!item.imagen && 
                        <div class="w-12 h-12 bg-gray-200 rounded-full flex items-center justify-center text-xl font-semibold text-gray-500">
                            {item.nombreCompleto[0]}
                        </div>
                    }
                    {item.imagen &&                     
                        <img src={item.imagen} alt="imagen del usuario" class="w-12 h-12 bg-gray-200 rounded-full flex items-center justify-center text-xl font-semibold text-gray-500">
                    }
                    <div class="ml-4">
                    <div class="font-bold text-gray-700">{item.nombreCompleto}</div>
                    <div class="text-sm text-gray-500">{item.localidad}</div>
                    </div>
                </div>
                <Stars rating={item.estrellas} />
                <p class="text-gray-700">
                    {item.opinion}
                </p>
            </div>
        ))}
    </div>
</div> -->